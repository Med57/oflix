{% extends 'back/base_back.html.twig' %}

{% block title %}Season{% endblock %}

{% block body %}
    <h1>Season</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ season.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ season.title }}</td>
            </tr>
            <tr>
                <th>NbEpisode</th>
                <td>{{ season.nbEpisode }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_season_index') }}">back to list</a>

    <a href="{{ path('app_back_season_edit', {'id': season.id}) }}">edit</a>

    {{ include('back/season/_delete_form.html.twig') }}
{% endblock %}
